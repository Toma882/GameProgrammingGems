<UserControl x:Class="GPGems.Visualization.Boids.BoidsCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600"
             Background="#1a1a2e">
    <Grid>
        <Canvas x:Name="DrawCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <!-- FPS 显示 -->
        <TextBlock x:Name="FpsText" Text="FPS: 0"
                   Foreground="White" FontSize="12"
                   Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top"/>

        <!-- 统计信息 -->
        <StackPanel Margin="10" HorizontalAlignment="Right" VerticalAlignment="Top">
            <TextBlock x:Name="BoidCountText" Foreground="White" FontSize="12"/>
            <TextBlock x:Name="SpeedText" Foreground="White" FontSize="12"/>
        </StackPanel>
    </Grid>
</UserControl>
